{
  "name": "raylib-js",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "compile:textures": "bun scripts/compile-textures.js",
    "test": "bun test tests/",
    "test:watch": "bun test tests/ --watch",
    "example:basic": "bun --hot examples/basic.ts",
    "example:shapes": "bun --hot examples/shapes.ts",
    "example:mouse": "bun --hot examples/mouse-input.ts",
    "example:multiple-textures": "bun --hot examples/multiple-textures.ts",
    "example:texture-manager": "bun --hot examples/texture-manager-demo.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
